<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Répertoire cache de l'application -->
    <cache-path name="cache" path="." />
    
    <!-- Répertoire de données de l'application -->
    <files-path name="app_data" path="." />
    
    <!-- Répertoire externe (si accessible) -->
    <external-path name="external" path="." />
    
    <!-- Stockage externe pour les fichiers partagés -->
    <external-files-path name="external_files" path="." />
    
    <!-- Répertoire de cache externe -->
    <external-cache-path name="external_cache" path="." />
</paths>